<header>
    <h2>codeial</h2>
    <ul>
        <% if (locals.user) {%>
            <li><%= user.username %></li>
            <li>
                <form action="/users/sign-out" method="post">
                    <button>Sign out</button>
                </form>
            </li>
        <% } else { %>
                <li>
                    <a href="/users/sign-in" method="post">Sign In</a>
                </li>
                <li>
                    <a href="/users/sign-up">Sign Up</a>
                </li>
            <% } %>
    </ul>
</header>